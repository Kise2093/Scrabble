#include <stdio.h>
#include <string.h>
#include <stdlib.h>


int main()
{
  printf("Welcome to Scrabble Game!!!\n");

  while(1)
  {
    if(strcasecmp(command,"resume"));
      resume();
    if (strcasecmp(command,"play"))
      play();
    }
  }

}
